set alias_file          = "~/.mutt/aliases"
set auto_tag
set beep_new
set certificate_file    = "~/.mutt/certificates"
set check_new
set copy
set date_format         = "%d %b %Y %H:%M"
set delete
set edit_headers        = no
set fast_reply
set forward_quote
set folder              = "~/mail"
set header_cache        = "~/mail/.header_cache"
set hide_thread_subject = no
set index_format        = " %-45.45s  %-32.32L  %> %[%d %b %Y] [%Z]"
set mail_check          = 10
set markers             = no
set mbox_type           = Maildir
set mailcap_path        = "~/.mutt/mailcap"
set message_cachedir    = "~/.mutt/body_cache"
set mime_forward        = no
set mime_forward_rest
set help                = no
set pager_stop
set postponed           = "~/mail/drafts"
set query_command       = "abook --mutt-query %s"
set rfc2047_parameters
set record              = "~/mail/sent"
set reverse_name
set sendmail            = "msmtp"
set sleep_time          = 0
set send_charset        = "us-ascii:utf-8"
set sort                = threads
set sort_aux            = reverse-last-date-received
set spoolfile           = "+inbox"
set sidebar_delim       = "  "
set sidebar_width       = 24
set sidebar_visible
set sidebar_folderindent


auto_view text/html
alternative_order text/enriched text/plain text/html text

macro attach	  s	"<save-entry><kill-line>~/downloads/<enter>y" "Save attachment"
macro index 	  ga	"<change-folder>=archive<enter>" "Go to all mail"
macro index 	  gd	"<change-folder>=drafts<enter>" "Go to drafts"
macro index 	  gi	"<change-folder>=inbox<enter>" "Go to inbox"
macro index 	  gs	"<change-folder>=sent<enter>" "Go to Sent Mail"
macro index 	  gt	"<change-folder>=trash<enter>" "Go to Trash"
macro index 	  Y	"!isync mail/inbox<enter>" "Sync with isync"
macro index,pager d	"<save-message>=trash<enter><enter><sync-mailbox>" "Trash"
macro index,pager y	"<save-message>=archive<enter><enter><sync-mailbox>" "Archive"
macro pager       q 	"<exit><sync-mailbox>"


bind editor <space>   noop
bind index gg first-entry
bind index G  last-entry
bind index z  collapse-thread
bind index Z  collapse-all
bind pager k  previous-line
bind pager j  next-line
bind pager gg  top
bind pager G  bottom
bind index,pager R   group-reply

bind index K  sidebar-prev
bind index J  sidebar-next
bind index L  sidebar-open
macro index,pager S "<enter-command>toggle sidebar_visible<enter>"

subscribe awesome@naquadah.org

source ~/.mutt/mboxes
source ~/.mutt/colors

folder-hook lists.*  source ~/.mutt/list.profile
folder-hook !lists.* source ~/.mutt/main.profile
