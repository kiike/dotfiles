#!/bin/sh

if  [[ $1 == play ]] && ! pgrep -u $UID moc; then
	notify-send "Spawning a mocp instance"
	sakura -e mocp
fi

case $1 in
	pause) mocp -G ;;
	next)  mocp -f ;;
	prev)  mocp -r ;;
	stop)  mocp -s ;;
esac

# Update widgets and notifications
player-notify
